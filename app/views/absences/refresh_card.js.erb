$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$("#day-card").html("<%= escape_javascript ( render partial: 'absences/day_card', locals: {date: @date, schedules: @schedules} ) %>");
