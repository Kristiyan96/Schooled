= link_to(school_group_marks_path(@school, @group, course_id: course.id), class: "col-md-6 col-xs-12 col-lg-4") do 
    .card.card-stats
      .card-header.card-header-default.card-header-icon
        b.card-icon
          = course.subject.name
        p.card-category = @group.teacher.full_name
