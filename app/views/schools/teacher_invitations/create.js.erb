closeModals();
$('#add-teacher .card-body input').val('');
$('#teachers').html("<%= escape_javascript ( render partial: 'schools/teacher_invitations/teacher_row', collection: @teachers, as: :teacher ) %>");