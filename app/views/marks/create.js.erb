$('#student-<%= @mark.student_id %> .whole-grade').val('');

if ("<%= @mark.id %>") {
  $("#<%= @mark.kind %> #student-<%= @mark.student_id %> .marks").append("<%= escape_javascript ( render partial: 'marks/mark', locals: {mark: @mark} )%>");
  $("#<%= @mark.id %>").hide().fadeIn(400);
}

